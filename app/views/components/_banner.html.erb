<%# locals: (attached: false, img_path: asset_path("banner.jpg"), title: "Hello") %>
<div class="banner">
  <img class="<%= attached ? "attached" : "default" %>" src="<%= img_path %>">
  <div class="overdelay"></div>
  <div class="title">
    <h3><%= title %></h3>
  </div>
</div>