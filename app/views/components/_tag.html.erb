<%# locals: (tags: nil) %>
<% tags.each do |tag| %>
  <span class="inline-block px-3 py-1 text-sm font-semibold text-gray-700 dark:text-gray-300 bg-gray-300 dark:bg-gray-700 rounded-full"><%= tag.name %></span>
<% end%>
